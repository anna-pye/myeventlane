thank_you:
  use_event_field: true
  event_field: 'field_thank_you_path'  # if present on Event, we use it
  default_path: '/thanks'
  keep_message: true
  utm:
    enabled: true
    source: 'myeventlane'
    medium: 'rsvp'
    campaign: 'event_{nid}'
