

<article class="{{ attributes.class }}">
<section class="event-card-buffered">
  {{ title_prefix }}
  {% if label %}
    <h1>{{ label }}</h1>
  {% endif %}
  {{ title_suffix }}
  {{ content }}
</section>
</article>
