{# views-view-unformatted--commerce_cart_form--commerce_cart_form.html.twig #}
<div class="mel-cart">
  {% for row in rows %}
    <div class="mel-card mel-cart-item">
      {{ row.content }}
    </div>
  {% endfor %}
</div>
